#pragma once

#include "fastfall/util/math.hpp"

namespace ff {

/*
class Attachable {
    virtual void attach_update(Vec2f ppos, Vec2f cpos, Vec2f vel) = 0;
    virtual Vec2f attach_get_pos() const = 0;
};
*/

}